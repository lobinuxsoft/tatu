target/
src-tauri/gen/
Cargo.lock
dist/
.tools/
.local-ct/
launcher/.godot/

# Built by CI's release job (godot --export-release), never committed:
# GitHub blocks any single git-committed file over 100MiB, and the stock
# Godot export template alone is already ~74-110MB before a single project
# asset. See tauri.conf.json's bundle.resources for how Tatu finds these
# at runtime.
src-tauri/vendor/launcher/
