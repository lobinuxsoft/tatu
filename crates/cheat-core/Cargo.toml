[package]
name = "cheat-core"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
nix = { version = "0.29", features = ["uio", "process"] }
procfs = "0.17"
bytemuck = { version = "1", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
