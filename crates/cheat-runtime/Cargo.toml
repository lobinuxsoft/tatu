[package]
name = "cheat-runtime"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
memchr = "2"
nix = { version = "0.30", features = ["uio", "process"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
