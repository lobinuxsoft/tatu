[package]
name = "cheat-runtime"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
dirs = "6"
memchr = "2"
nix = { version = "0.30", features = ["uio", "process"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
